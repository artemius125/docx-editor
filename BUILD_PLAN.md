# План сборки

## Мерило

Агент принимает `.docx` и правки обычным языком, вносит их и возвращает
файл плюс отчёт: что сделано, что не вышло и почему. Готовность считаем
по паре `/home/artem/Загрузки/Правки_ColBERT_20.md` (20 правок) +
`/home/artem/Загрузки/Архитектура_ColBERT.docx` (116 абзацев).

Не «20 из 20 выполнено» — это недостижимо и не требуется заказчиком.
Готовность = по каждой из 20 правок система выдала честный вердикт, и
вердикт совпадает с тем, что реально в файле.

## Конвейер

```
правка (одна из списка)
   │
   ├─ Навигатор  (LLM, ~300 токенов: оглавление + текст правки)
   │     → {kind, rule, anchors}
   │
   ├─ Поиск      (код: якоря → блоки → фрагмент с соседями)
   │
   ├─ Редактор   (LLM, ~600 токенов: фрагмент + текст правки)
   │     → патч JSON
   │
   ├─ Валидатор  (код: id есть, old≠new, old найден)
   │
   ├─ Применение (код: правка живых lxml-элементов на месте)
   │
   └─ Проверка   (LLM, ~400 токенов: diff изменённых блоков + текст правки)
         → принято | откат снимка + причина
```

Правки типа `rule` (типографика, кавычки) идут мимо Редактора: Навигатор
называет правило, код его выполняет.

## Контракты (заданы снаружи, не изобретать)

**Блок** — то, что отдаёт `parse.doc_map()` и потребляет веб-интерфейс:
`{"id","kind":"p","style","text","align","runs":[{"text","b","i","u"}]}`
для абзаца, `{"id","kind":"t","rows":[[...]]}` для таблицы. Формат
зафиксирован `web/index.html` — менять его нельзя, интерфейс переиспользован
из прошлого проекта как есть.

**Эндпоинты** (тоже из `web/index.html`): `POST /upload`, `POST /edit`
(поток NDJSON), `GET /preview/{session}`, `GET /download/{session}`,
`GET /logs`.

**Событие потока `/edit`**: как в прошлом проекте — `{"type":"op","status",
"text","task","task_text","model","at","dt","blocks"}` — плюс новое поле
`"verdict"`: `"done"` | `"failed"` | `"rolled_back"`.

**Патч** — список операций. Ровно эти, новых не изобретать; если правка не
выражается ими, это честный отказ, а не повод дописать операцию:

```
{"op":"replace_text","id":"p12","old":"...","new":"..."}
{"op":"set_text","id":"p12","text":"..."}
{"op":"insert_after","id":"p12","text":"...","style":"Normal"}
{"op":"delete","id":"p12"}
{"op":"move_after","id":"p12","after":"p19"}
{"op":"set_style","id":"p12","style":"Heading 1"}
{"op":"create_table","after":"p57","rows":[[...]],"header":true}
{"op":"set_cell","id":"t3","row":0,"col":1,"text":"..."}
{"op":"normalize","rule":"typography"|"quotes"}
{"op":"replace_all","old":"...","new":"..."}
```

## Очередь работ

Строго сверху вниз. Пункт закрыт демо с assert'ами (см. CLAUDE.md).

### Ф1. Каркас
- [x] `parse.py` перенесён из прошлого проекта как есть — проверить, что
      `index/doc_map/render` работают на `Архитектура_ColBERT.docx`
      (116 блоков, 0 таблиц, все `Normal`).
- [x] `server.py`: пять эндпоинтов, `/edit` пока возвращает поток из одного
      события-заглушки. Веб-интерфейс открывается, документ грузится,
      превью рисуется, скачивание отдаёт валидный файл.
- [x] `examples/run_all.py` — параллельный тихий раннер демо, печатает
      только упавшие. Не больше 40 строк.

### Ф2. Патч и валидатор
- [x] `patch.py`: `validate(blocks, op, doc) -> None | текст ошибки` и
      `apply(doc, idx, op) -> описание`. Валидация ДО применения:
      несуществующий id, `old == new`, `old` не найден в блоке, неизвестный
      стиль (в ошибку — список доступных стилей документа).
- [x] Операции из контракта выше, кроме `normalize` (она в Ф3).
      Правка текста — через раны, форматирование абзаца сохраняется.
- [x] Демо: каждая операция применяется; каждый вид невалидного патча
      отбивается валидатором с внятным текстом.

  Находка: `validate` в итоге принимает три аргумента — `(blocks, op, doc)`,
  а не два. Причина: список «доступных стилей абзацев» для ошибки
  `set_style` должен браться из `doc.styles` (полный каталог стилей,
  определённых в документе), а не из `blocks` — там только стили, УЖЕ
  применённые к каким-то абзацам. Проверено на реальном документе:
  `Архитектура_ColBERT.docx` использует только `Normal` во всех 116
  абзацах (см. Ф1), но в `doc.styles` определён `List Paragraph` (и НЕТ
  `List Bullet`) — без доступа к `doc` эта проверка невозможна в принципе.
  Контракт-строка выше в этом файле поправлена соответственно.

  Ревью архитектора нашло три дефекта, все починены: (1) `_ptext`/
  `_replace_span` ходили только по ранам верхнего уровня (`r_lst`) — текст
  внутри `w:hyperlink` выпадал, из-за чего `apply` видел офсеты, не
  совпадающие с `p.text` (то, что видит `validate`), и молча портил абзац;
  теперь обход через `p_el.iter(qn("w:r"))`, плюс громкая ошибка при
  недостижимом отрицательном смещении. (2) `insert_after` с несуществующим
  стилем проходил `validate` и падал `KeyError` в `apply` — вынесена общая
  проверка стиля `_style_error`, теперь зовётся и из `set_style`, и из
  `insert_after`. (3) `create_table` строил таблицу без рамок (в контракте
  нет поля `style`, а из именованных стилей часто есть только `Normal
  Table`) — рамки теперь ставятся всегда прямо в XML (`_add_borders`,
  `w:tblBorders`). Демо дополнено тремя проверками (гиперссылка, плохой
  стиль в `insert_after`, `w:tblBorders` в XML), все падают при регрессии.

### Ф3. Правила без модели

Числа ниже замерены архитектором на `Архитектура_ColBERT.docx` — это
приёмочные значения, а не ориентиры.

- [x] `rules.py`: `typography` — три правила по всему документу:
      - пробел перед пунктуацией: любой пробельный, включая U+00A0
        (в документе 6 мест перед двоеточием, и все шесть — именно U+00A0,
        обычных нет ни одного; плюс 1 место перед закрывающей скобкой);
      - пропущенный пробел после точки между предложениями. Правило:
        **слева буква ЛЮБОГО регистра, справа заглавная**; от ложных
        срабатываний защищает запрет цифр по обе стороны от точки, а не
        регистр. Так надо, потому что из трёх случаев в документе
        («в RAG.Усредненный», «процесс.Исследователи», «напрямую.В основе»)
        первый имеет слева ЗАГЛАВНУЮ `G`, и правило «слева строчная»
        пропустило бы треть случаев молча. Ложные срабатывания здесь —
        только десятичные числа (`4.2`, `0.4448`), их и отсекаем;
      - невидимые U+2060/U+200B/U+FEFF/U+00AD — вырезать. В документе их
        ноль, живого случая нет: демо для этого правила — на синтетике.
- [x] `quotes` — прямые кавычки в ёлочки (в документе 22 прямых при 13
      парах уже готовых ёлочек; вложенных кавычек внутри ёлочек нет).
- [x] Демо на реальном `Архитектура_ColBERT.docx`: после `typography`
      не осталось ни одного пробела перед пунктуацией и все 3 слипшихся
      предложения разделены, при этом 6 десятичных чисел (`4.2%`, `0.4448`,
      `0.4436`, `8.8`, `36.2%`, `6.7%`) не тронуты; после `quotes` прямых
      кавычек 0, а 13 исходных пар ёлочек целы.

  Находка: `rules.py` импортирует `_ptext`/`_replace_span` из `patch.py` (не
  дублирует их), а `patch.py` вызывает `rules.typography`/`rules.quotes` из
  `_op_normalize` — прямой импорт на верхнем уровне модуля дал бы цикл, поэтому
  импорт `rules` внутри `patch._op_normalize` сделан ленивым (внутри функции).
  Числа архитектора (7 пробелов = 6 U+00A0 перед `:` + 1 перед `)`; ровно 3
  слипшихся предложения теми же тремя строками; 6 десятичных чисел встречаются
  по одному разу; 22 прямых кавычки, 13 исходных пар ёлочек) перепроверены на
  реальном файле до написания кода — совпали ровно, расхождений нет.

### Ф4. Поиск

Ставка «поиска подстрокой хватит» проверена архитектором на всех 20
правках: якорь нашёлся у каждой, провалов ноль — эмбеддинги не нужны.
Но качество якоря решает всё: обобщённый термин даёт мусор («MaxSim» →
25 блоков из 116), а дословная цитата из формулировки правки схлопывает
выборку до одного-четырёх («размерностью m × n» → 1, «ColBERT-Att» → 4,
«Эффективность архитектур: Сравнительный анализ» → 1). Отсюда требование
к Навигатору в Ф5: якорь — самая специфичная ДОСЛОВНАЯ цитата из текста
правки, а не термин своими словами.

- [x] `find.py`: `by_text(blocks, needle)`, `by_regex(blocks, pattern)`,
      `first_mention(blocks, term)` (для «расшифруй при первом упоминании»),
      `outline(blocks)` (заголовки + первые слова абзацев верхнего уровня,
      для контекста Навигатора), `fragment(blocks, ids, around=1)`.
- [x] Демо: фикстура из 20 наборов якорей (по одному на каждую правку) —
      для каждого поиск находит непустой набор блоков, и он не больше 20.

  `outline`: в приёмочном документе заголовков нет вовсе (все 116 абзацев
  стилем Normal — см. Ф1), поэтому оглавление устроено как id + обрезанный
  до 20 знаков префикс текста для обычных абзацев (Heading*/Title — целиком,
  на будущее для других документов). На `Архитектура_ColBERT.docx` даёт
  2972 знака на 116 блоков — заведомо укладывается в промпт Навигатора
  (~300 токенов по бюджету конвейера).

  Находки по ходу фикстуры (20 якорей из `Правки_ColBERT_20.md`, все
  проверены на реальном `Архитектура_ColBERT.docx`):

  1. В документе U+00A0 встречается не только перед пунктуацией (это уже
     фиксировала Ф3), а вообще между случайными словами середины
     предложения — например `Оператор\xa0$\top$\xa0представляет`,
     `как\xa0и,`, `секундами\xa0)`. Дословная цитата из правки, набранная
     обычными пробелами, не матчится как подстрока, если задевает такую
     границу. Якоря для правок 1, 2 и 17 пришлось обрезать ДО первого nbsp
     в цитате, а не после — иначе `by_text` возвращает пустой список на
     формально «дословной» цитате.

     Ревью архитектора замерило масштаб точно: **61 из 116 абзацев
     содержат U+00A0 внутри предложения, 147 вхождений всего** — это
     большинство документа, а не редкая ловушка, и она бьёт дважды:
     в якорях Навигатора и в поле `old` операций `replace_text`/
     `replace_all`. Промптом не лечится — модель не видит разницы между
     обычным пробелом и U+00A0 в тексте, который ей показывают. Починено
     в коде, в одном месте: `find._flex_span(haystack, needle)` — сначала
     точный `str.find` (быстрый путь, точные совпадения не меняются),
     и только при промахе needle режется по пробельным разрывам
     (`re.split(r"\s+", needle)`) и собирается в regex, где любой такой
     разрыв матчится с любым другим (`\s` в Python уже покрывает U+00A0).
     Публичный `find.flex_find` отдаёт индекс начала; `_flex_span` — ещё и
     конец РЕАЛЬНОГО совпадения, а не `start + len(needle)`: разрыв в
     документе может быть длиннее или короче того, что в needle.

     `by_text` использует `flex_find`. `patch.validate` (проверка «текст
     не найден» для `replace_text`/`replace_all`) и `patch._op_replace_text`/
     `_op_replace_all` (реальное вычисление спана для замены) используют
     ОДНУ и ту же `_flex_span` из `find.py` — не дублируют её и не могут
     разойтись, это именно тот класс бага, что уже портил гиперссылки в Ф2.
     Демо: `patch_demo.test_replace_text_tolerates_nbsp` — абзац
     `"слово\xa0слово синее небо."`, `old` набран обычным пробелом,
     результат проверен целиком (не только начало), плюс отдельно проверено,
     что точное совпадение (быстрый путь) не сломано; `find_demo.
     test_by_text_tolerates_nbsp` — та же цитата про `Оператор`, что и в
     примере архитектора, находит `p31` через обычные пробелы.
  2. Формулы — отдельная ловушка: правка 16 цитирует «размерностью m × n»
     (юникодный знак умножения, как выглядит в размеченном тексте правки),
     а в документе это `$m \times n$` (сырой LaTeX-макрос, буквально
     обратный слэш и `times`). Подстрокой это не совпадает никогда. Якорь
     для правки 16 взят из текста ДО формулы («матричных умножений
     MaxSim»), не из самой формулы.
  3. У трёх правок из 20 в тексте самой правки нет ни одной дословной
     цитаты документа — это не промах `find.py`, а честная граница метода:
     - правка 6 (кавычки в ёлочки) — глобальное типографическое правило,
       оно и не должно идти через `find`: Навигатор называет `rule`, код
       применяет его по всему документу (см. «Конвейер» — правки типа
       `rule` идут мимо Редактора);
     - правка 9 («заголовок документа») — правка по позиции, а не по
       тексту: цель — первый блок документа, а не что-то, что можно
       процитировать;
     - правка 15 (вложенные характеристики в списке) — это уже описанный
       выше в «Известных рисках» невыполнимый случай (в документе нет
       списков вообще); отсутствие цитаты — ещё один взгляд на ту же
       находку архитектора, не новая проблема.
     Демо фиксирует именно эти три номера явным `assert`, чтобы если
     список изменится при доработке — это было явным сигналом регрессии,
     а не тихо пропавшей проверкой.

### Ф5. Цикл правки

Оба вызова модели архитектор проверил живьём на Gemma 4 26B до написания
кода. Результаты ниже — измеренные, промпты в них уже отлажены.

**Проверяющий работает и это подтверждено.** Четыре случая по правке 11,
все четыре вердикта верны: правка сделана верно → `ok=true`; сделано
наоборот (ровно тот провал, на котором соврал прошлый проект) → `ok=false`
«выполнена наоборот»; пустышка `old==new` → `ok=false` «правка не
выполнена»; изменён не тот термин → `ok=false`. 0.01–0.02 ₽ за проверку.
Асимметрия «проверять легче, чем делать» на 26B держится — на ней и стоит
обещание честного отчёта.

**Навигатор ошибается, и конвейер обязан это переживать.** На шести
представительных правках сырой промпт дал 2 верных ответа из 6,
отлаженный — 5 из 6. Что дало разницу и должно попасть в реализацию:

- В ответе есть поле `ids` рядом с `anchors`: `{"kind","rule","ids","anchors"}`.
  Модель и так пытается называть блоки, но без этого поля засовывает id
  внутрь цитаты (`"p51 Bi-Encoders (Dense Embeddings)"`), и якорь не
  находит ничего. С отдельным полем правка 19 дала 3 верных блока вместо
  нуля. Плюс защита в коде: срезать ведущий `pNN ` из якоря.
- Правила описаны конкретикой, а не названием: «лишний пробел перед
  двоеточием / точкой с запятой / закрывающей скобкой», «слипшиеся
  предложения», «невидимые символы» — иначе правка 2 не опознаётся как
  `typography` и уходит к модели вместо кода.
- `global` запрещено использовать как свалку: «если можешь назвать хотя бы
  один id — это не global, а local». Без этой фразы правка 9 падала в
  `global` и теряла адрес.
- Якоря рядом с формулами цитируют прозу, а не формулу: правка 16 пишет
  `m × n`, а в документе лежит `$m \times n$` — это никогда не совпадёт.

- [x] Запасной путь при промахе Навигатора (обязателен, не опция).
      Если не названо ни одного существующего `id` и ни один якорь ничего
      не нашёл — код ищет сам по дословным цитатам из формулировки правки
      (заказчик щедро даёт их в «ёлочках»). Если и это пусто — честный
      отказ «не нашёл, где это править». Промахнувшийся Навигатор обязан
      приводить к внятному «не смог», а не к тихому «сделано».
- [x] `llm.py`: один вызов модели, `.env`, JSON-режим, разбор ответа.
      Меньше 60 строк, без ретраев и без абстракций провайдера.
- [x] `edit.py`: Навигатор → поиск → Редактор → валидатор → применение →
      Проверка → принято/откат. Снимок = копия файла перед правкой, откат =
      перечитать документ из копии. Максимум две попытки, потом честный
      отказ с причиной.
- [x] `split(request)` — детерминированное деление списка правок по
      markdown-нумерации (перенести из прошлого проекта, он там работал).
- [x] Демо: правка 11 из файла правок проходит и не делает `old == new`;
      заведомо невыполнимая правка («поставь нумерацию страниц») даёт
      честный отказ и не трогает документ.

  Находки по ходу (задание строителю уточнило детали демо по сравнению с
  формулировкой выше — задокументировано, а не переоткрыто):

  1. Живой вызов в демо — ровно один, и это правка 12 («на порядок (в
     6–10 раз)»), а не 11: у 12 заранее измерен надёжный якорь (Навигатор
     стабильно возвращает `p76`), у 11 такого измерения для связки
     Навигатор+Редактор не было (архитектор измерял для неё только
     Проверяющего, отдельно, четырьмя фейковыми диффами). Прогнано дважды
     подряд — оба раза `verdict=done`, `p76` реально меняется. Отказ
     («поставь нумерацию страниц») в демо проверен БЕЗ живого вызова —
     фейковым Навигатором, который целится в несуществующий `id` и
     промахивающийся якорь, а текст правки несёт `«цитату»`, которой тоже
     нет в документе. Это бьёт ровно в требуемый путь кода (резолв →
     запасной поиск по «ёлочкам» → пусто → честный отказ), просто без
     траты вызова модели и без риска, что конкретно «нумерация страниц»
     когда-нибудь случайно попадёт в `ops` живого Редактора.
  2. Найден живой баг Редактора: на 26B модель иногда кладёт в `"ops"`
     посторонний примитив первым элементом рядом с валидными операциями
     (`{"ops": [0, {"op": "replace_text", ...}, {"op": "replace_text", ...}]}`
     — воспроизведено на правке 12). `patch.validate` ожидает словарь и
     падал `AttributeError` на `0.get`. Почин в `edit.py`: `_clean_ops`
     отбрасывает элементы `ops`, которые не словари, ДО `validate` — это
     не проглатывание ошибки (инвариант 6), а нормализация плохо
     сформированного, но частично разбираемого ответа модели перед тем,
     как код станет его проверять по существу.
  3. Поле `kind` в ответе Навигатора (`"local"|"global"`) в код маршрутизации
     не заведено намеренно: оно не упоминается в разделе «Routing» задания
     буквально (там ветвление только по `rule`/`ids`/`anchors`), а
     дисциплинирующий эффект («если можешь назвать хоть один `id` — это
     local, а не global») достигается формулировкой в промпте Навигатора,
     а не отдельным `if kind == ...` в коде. `kind` разбирается из ответа,
     но не влияет на резолв: полноценная отдельная ветка ради поля,
     которое ничего не решает, была бы абстракцией с одним вызывающим.
  4. Снимок для отката не требует пути исходного файла на диске: `doc` —
     объект `python-docx` в памяти, поэтому `_snapshot`/`_restore` в
     `edit.py` используют `tempfile.mkstemp` + `doc.save()`/`Document()`
     сами по себе, без параметра `path` в `run_edit`. Побочный эффект,
     который стоит явно отметить: живой прогон демо ни разу не пишет
     поверх `/home/artem/Загрузки/Архитектура_ColBERT.docx` — документ
     только читается один раз в память, а откат/снимок работают через
     временный файл, так что мерило приёмки не портится повторными
     запусками `run_all.py`.
  5. Ф5 переоткрывалась на Ф6 и починена там же (два дефекта, оба вскрыты
     журналом из Ф6 — поле `reply` окупилось в первый же прогон):
     - Навигатор вернул `ids: [112]` ЧИСЛОМ, а не `"p112"`. `_resolve`
       сравнивал с множеством строк и молча терял id, резолв выживал только
       на якоре и уезжал не туда. Это тот же класс, что и «id внутри цитаты»:
       каноническую форму id приводит КОД, а не промпт. Добавлен
       `_normalize_id` (голое число → `pNN`, регистр вниз), сверка с
       реальными id осталась строгой — фаззи-совпадений не заводить.
     - Живое демо требовало `verdict == "done"` от 26B, то есть краснело от
       настроения модели. По «Мерилу» готовность — честный вердикт,
       совпадающий с файлом, а не «сделано»: теперь `done` обязан означать
       реальное изменение p76, а `failed`/`rolled_back` — нетронутое
       содержимое и непустую причину. Байтовое сравнение `.docx` для этого
       НЕ годится: `python-docx` пишет zip через `writestr`, тот штампует
       записи текущим временем, и один и тот же документ даёт разные байты
       (измерено). Сравнивается содержимое блоков через `_btext`.

### Ф6. Интерфейс
- [x] `/edit` включён на настоящий цикл, в событие добавлен `verdict`.
      Документ сохраняется на диск ПОСЛЕ КАЖДОЙ правки, а не в конце потока:
      иначе падение на правке N обнуляет уже отрапортованные `done` правки
      1..N-1 и `/download` отдаёт файл без них (инвариант 5). Закреплено в
      демо третьим сценарием — фикстура роняет Навигатора на второй правке,
      проверяется, что исключение долетает наружу, а первая правка уже на
      диске. Мутационная проверка: с `save` после цикла демо падает.
- [x] `web/index.html`: показ вердикта в строке правки (`renderOpLine`) и
      в итоге (`renderAgentMsg`). Правка десятка строк, не переписывание.
      `rolled_back` отделён от `failed`: пометка «откат» в строке и отдельный
      счётчик в сводке («сделано N, не удалось M, откачено проверкой K») —
      сервер кладёт откаты в `failed`, и без этого честная работа Проверяющего
      читалась бы как провал. Оговорка: `examples/web_demo.py` — рот-аларм по
      тексту исходника, JS нигде не исполняется (node в окружении нет), так
      что опечатка внутри строки вёрстки демо не поймает.
- [x] `log.py` — JSONL, одна запись на правку: что просили, что нашли, что
      ответила модель, вердикт проверки, причина. `GET /logs` отдаёт хвост.
      Имена полей взяты из уже написанного потребителя (`renderLogRecord`):
      `task, task_text, model, iter, verdict, reason, ids, reply`. Два поля
      потребовали протяжки через `run_edit`, и она разрешена архитектором:
      `reply` = `{"nav":…, "editor":…}` (разобранный JSON ролей, сырого тела
      HTTP нигде нет; `editor: null` — Редактора не звали, путь `rule`),
      `iter` = настоящее число попыток из `_apply_ops` (1 или 2), а не
      константа: панель, врущая «итерация 1» на правке с ретраем, стоит
      времени отладки ровно там, где его меньше всего. Оговорка: при ретрае
      в `reply` лежит ПЕРВЫЙ (отклонённый) ответ Редактора — исправленный
      не логируется; `iter: 2` рядом показывает, что был второй.

### Ф7. Приёмка
- [x] `examples/colbert_run.py`: 20 правок поверх настоящего документа,
      печать вердикта и причины по каждой, проверка целостности файла.
      Сохранение после каждой правки, сравнение «до/после» упорядоченным
      списком текстов блоков (id нумеруются позиционно, после отката они
      переезжают — словарь по id дал бы выдуманные «нарушения»), обрыв
      транспорта = отдельный исход `crashed` и громкий провал прогона.
      Два первых прогона умерли на сети (обрыв на 10-й и 11-й правке);
      отсюда ретрай в `llm.py` и сохранение поединично.
- [x] Разбор результата архитектором, находки — новыми пунктами.

**Результат прогона (20/20, 305 с): done 6, failed 12, rolled_back 2,
crashed 0.** Проверено архитектором НЕЗАВИСИМО от раннера, сравнением
исходного документа с `out/colbert_result.docx`:

- изменились ровно 24 блока, и все 24 покрываются объединением `ids` шести
  правок с вердиктом `done` — ни одного изменения «мимо отчёта»;
- блоки откаченных правок (`p11`, `p19`, `p102`, `p54`) побайтово как в
  исходнике — откат работает на живом прогоне, а не только в фикстуре;
- правка 1 — тот самый провал, на котором соврал прошлый проект: модель
  вставила `[CLS]` вместо `[SEP]`, Проверяющий поймал, код откатил;
- правка 15 отказала ровно по предсказанию раздела «Известные риски».

### Ф8. Находки приёмки (по порядку важности)

- [x] **Обрезание абзаца проходит как `done` — единственная найденная ложь
      отчёта.** Правка 18 (`p35`): модель выдала `set_text`, укоротив абзац
      с 456 до 332 знаков и оборвав последнее предложение буквальным «…»
      («…winner-takes-all) карди…»). Смысловая часть правки сделана верно,
      поэтому Проверяющий сказал `ok`, а машинная сверка «содержимое
      изменилось» такое не ловит по устройству. Это потеря текста, выданная
      за успех. Чинить двумя независимыми слоями:
      1) детерминированно в `patch.validate` — запретить `set_text`/
         `replace_text`, где `new` оканчивается многоточием, а `old` нет
         (в приёмочном документе таких абзацев 0, ложных срабатываний нет);
      2) показывать Проверяющему длины: в диффе к каждому блоку добавить
         «было N знаков, стало M» — модель хорошо ловит «стало вдвое
         короче», когда видит числа, и плохо, когда не видит.
- [x] **Правка типа `rule`, ничего не изменившая, обязана давать «уже
      выполнено», а не «не удалось».** Введён ЧЕТВЁРТЫЙ вердикт `already`
      («требуемое состояние уже в документе»), только для пути `rule`: на
      пути через Редактора пустой дифф остаётся `failed`, там из «операции
      ничего не изменили» не следует, что просьба выполнена. В потоке
      `/edit` идёт как `status: done`, в интерфейсе — пометкой «уже было». Правка 3 (слипшиеся предложения)
      получила `failed` «операции применились, но текст не изменился» —
      потому что типографику по всему документу уже применила правка 2.
      Требуемое состояние в документе ЕСТЬ, и отчёт «не удалось» вводит
      в заблуждение не меньше, чем ложное «сделано» (инвариант 5).
- [x] **Навигатор сваливает в `rule` то, что правилом не является.**
      Промпт Навигатора сужен до двух названных классов плюс отрицательный
      пример (единообразие написания термина — работа Редактора, не правило).
      Фикстурой промпт не доказывается: проверка — приёмочным прогоном.
      Правка 7 (четыре разных написания Bi-encoder → одно) уехала в
      `typography`, код прогнал нормализацию, ничего не изменилось, правка
      потеряна. `rule` обязан ставиться только для трёх названных
      типографских классов; «единообразие термина» — работа Редактора.
- [x] **Глобальные правки получают локальный фрагмент.** Поиск по «ёлочкам»
      из текста правки больше не запасной путь: он идёт ВСЕГДА и сливается с
      адресами Навигатора. Замерено на приёмочном документе: фрагмент правки 8
      вырос с 3 до 9 блоков, ни одна цитата не даёт больше одного блока —
      разрастания нет, ограничитель не нужен. Правки 4 и 14 этим не
      чинятся: в их тексте «ёлочек» нет вовсе. Правки 4, 8, 14
      отказаны с причиной вида «во фрагменте только один случай / этих
      чисел во фрагменте нет», хотя по всему документу случаи есть.
      Формулировка «по всему документу / везде» должна вести к сбору ВСЕХ
      совпадений (операция `replace_all` уже есть), а не к окну ±1 блок.
- [x] **Дыра в безопасности цикла при обрыве связи.** Вызов Проверяющего
      обёрнут: на любом исключении документ восстанавливается из снимка НА
      МЕСТЕ (мутируются те же объекты doc/idx — вызывающий держит старую
      ссылку, вернуть новую пару через исключение нельзя), снимок удаляется,
      исключение поднимается дальше без изменений. Если транспорт
      падает на вызове Проверяющего, патч уже применён к живому `doc`,
      снимок ещё не удалён: исключение улетает наверх, документ остаётся
      изменённым и НЕпроверенным, а временный файл снимка течёт. Нужен
      `try/finally` вокруг проверки в `run_edit`: на исключении — откат из
      снимка и повторный подъём ошибки (ошибка остаётся громкой).

### Ф9. Структура документа (найдено владельцем на показе)

Владелец сравнил наш просмотрщик с LibreOffice: у нас плоская простыня, у
него — заголовки и вложенные списки. Причина измерена: документ размечен
ПРЯМЫМ форматированием, а мы спрашиваем именованный стиль.

`w:pStyle` — 0 штук на весь документ (отсюда «все Normal»), при этом:
`w:outlineLvl` — 24 абзаца (уровень 0 — один, 1 — девять, 2 — четырнадцать);
`w:numPr` — 31 абзац, из них 7 с `ilvl=1`, то есть вложенные, в семи разных
нумерациях; `numbering.xml` в архиве есть. Обычных абзацев без структуры —
61 из 116.

**Это отменяет записанный ниже риск по правке 15.** Формулировка «в
документе НЕТ списков, маркеров списка ноль» — ложная, она получена из
`p.style.name`, а не из XML. Списки есть, вложенность есть. Отказ по
правке 15 остаётся верным, но по ДРУГОЙ причине: в наборе операций нет
смены уровня списка. Прежняя причина была неправдой о документе.

- [x] `parse.py`: блок абзаца получает `level` (из `w:outlineLvl`, иначе
      `None`) и `list` (`{"ilvl","numId"}` из `w:numPr`, иначе `None`).
      Существующие ключи не трогать — на них стоит интерфейс.
- [x] `web/index.html`: заголовки рисуются по уровню, элементы списка — с
      отступом и маркером по `ilvl`. Документ в просмотрщике обязан читаться
      так же структурно, как в LibreOffice.
- [x] `find.outline`: оглавление для Навигатора строится по `level`, а не по
      именам стилей `Heading*`, которых в этом документе нет ни одного —
      Навигатор всё это время работал по плоскому списку вместо 24 заголовков.
- [ ] Решить, нужна ли операция смены уровня списка (правка 15). Отдельным
      пунктом ПОСЛЕ трёх верхних: сперва увидеть структуру, потом решать.

### Ф10. Работа снаружи (найдено на показе)

- [x] **Поток `/edit` молчит во время правки и его рвёт облачный туннель.**
      Измерено на живом показе: у владельца поток оборвался «TypeError:
      network error» после пятого события. В журнале КОНТЕЙНЕРА при этом
      лежит запись о шестой правке — сервер её доделал, браузер уже не
      получил. Контрольный прогон через тот же туннель с быстрыми правками
      (событие каждые ~4 с) прошёл 22 события за 90 с без разрыва: режет не
      длительность запроса и не объём, а ПАУЗА между байтами. Долгая правка
      (кавычки по десяти блокам) эту паузу и создаёт. Чинится сердцебиением:
      пока считается правка, поток раз в несколько секунд шлёт событие-пинг.
      Клиент неизвестные типы событий игнорирует — интерфейс не трогать.
      Сделано: правка считается в отдельном потоке (`asyncio.to_thread`),
      раз в 5 с уходит `{"type":"ping"}`. Замерено на настоящем uvicorn с
      настоящим сокетом (ASGITransport для этого не годится — он буферизует
      весь ответ): максимальный разрыв между строками 5.0 с.
      Оговорка по истории: код этой правки попал в коммит c73b070 (Ф9) —
      архитектор собрал `git add -A` в общем дереве, пока правка лежала
      несохранённой. Сообщение того коммита её не описывает.
- [ ] **`GET /logs` без сессии отдаёт журнал ВСЕХ сессий.** Сервис выставлен
      в интернет без аутентификации: чужие документы, запросы правок и ответы
      модели видны любому. Решение владельца о закрытии — ждём.

## ОТКУДА ПРОДОЛЖАТЬ (написано в конце сессии 2026-07-27)

**Стенд и все замеры лежат в `/home/artem/docx-editor-bench/`** — он вынесен из
временного каталога сессии специально, чтобы пережить её. Внутри `run.py`
(каждая правка на свежей копии), `run_seq.py` (весь список подряд),
`oracle.py`, `score.py`, `runs/` с прогонами `baseline`, `w4`, `w5`, `w6`, `w7`
и разбор чужой системы `superdoc-разбор.md`. Запуск:
`cd /home/artem/docx-editor-bench && BENCH_LABEL=имя python3 run.py colbert`.
Сравнение: `python3 score.py baseline w7` и `python3 score.py --seq baseline w7`.
Вывод НЕ фильтровать грепом: падение прогона пролезает сквозь фильтр молча,
на этом уже один раз потеряли диагностику.

**ПЕРВОЕ ДЕЙСТВИЕ: партия `w7` ЗАМЕРЕНА И НЕ ПРИНЯТА. Её надо разделить.**

Замер (изолированно, 40 правок): `done` 26 → **22**, оценщиком 13 → 12.
Минус четыре при пороге значимости «меньше трёх прироста не считается».
Перевернулось восемь: выиграли ColBERT 16 и Математика 18, проиграли
ColBERT 11, 12, 15, 19 и Математика 16, 20. Накопительный режим не мерился.

Показательнее всего **ColBERT 19**: была `done` с подтверждением оценщика
(настоящая таблица Word в XML) — стала `failed`. Одна из лучших правок Ф12.

Главный подозреваемый — **проверка полноты набора целей**: она объявляет
правку неполной, если хоть один кластер применился без ошибки, но не дал
изменения. Для многокластерных правок (таблица + подпись + ссылка в тексте)
это слишком строго: один пустой кластер топит правку, которая сделана.
Похоже, обменяли одну ложь на четыре потерянных правки. **Это гипотеза, а не
диагноз** — проверять на данных, как всё остальное.

Что делать:
1. Разделить `w7` на три и замерить порознь: (а) инвентарь вариантов,
   (б) обучающая ошибка самопопадания, (в) проверка полноты. Ошибка
   архитектора в том, что он сложил в одну партию изменения, бьющие по
   одному месту, — сам же и предупреждал против этого.
2. Ожидаемый исход: (а) и (б) полезны, (в) в нынешнем виде вредна. Смягчить
   (в) — считать неполной только правку, где кластер ПРОВАЛИЛСЯ, а не просто
   ничего не изменил.
3. Замерять НЕ после каждой правки (полный замер ~50 минут). Между
   изменениями — `run_all.py`, он бесплатный и офлайновый. Полный замер — на
   границе партии, и в партию складывать изменения, бьющие по РАЗНЫМ правкам.

**Состояние по числам** (изолированно / накопительно, из 40):
база 15 / 9 → Ф11 18 / 15 → **Ф12 26 / — (лучшее)** → Ф12+w7 22 / —.
ColBERT списком на Ф12: 11 из 20. Математика списком не досчитана из-за
переполнения контекста, см. Ф13-бис.

**Всё это уже в истории** (`c7ff5f1`, `5bbc172`): и Ф12, и непринятая `w7`, и
два новых op. Откатывать `w7` целиком не надо — надо разделить и перемерить,
код у неё правильный, вопрос в строгости одного правила.

**Ошибка архитектора, повторённая второй раз** (первый — на Ф10): `git add -A`
в общем дереве, пока агент ещё писал. Коммит `c7ff5f1` захватил `set_format`
на середине: операции попали в `_OPS`, обработчиков в `_HANDLERS` не было,
применение упало бы с `KeyError`. Починено в `5bbc172`. Проверка, которой не
было и которая ловит это мгновенно: **`_OPS - set(_HANDLERS)` обязано быть
пустым** — завести демо.

### Ф11. Замер как способ решать (стенд, а не впечатление)

Заведён измерительный стенд ВНЕ репозитория (`scratchpad/bench/`): 40 правок
двух корпусов, каждая на свежей копии (`run.py`) и весь список подряд по
одному документу (`run_seq.py`), плюс независимый оценщик по маркерам
(`oracle.py`) — он проверяет ФАЙЛ, а не вердикт системы. Правка кода
принимается, только если подняла число верных правок. Разброс между
прогонами ±2, поэтому прирост меньше трёх по агрегату ничего не доказывает —
атрибуция делается по конкретным перевернувшимся правкам.

Замеренный итог (было → стало):

| | изолированно | всем списком |
|---|---|---|
| выполнено | 15 → 18 из 40 | 9 → 15 из 40 |
| подтверждено оценщиком | 8 → 11 из 19 | 8 → 10 из 19 |
| ложных вердиктов | 2 → 0 | 2 → 1 |
| испорченных документов | 0 → 0 | 0 → 0 |

- [x] **`_diff` был слеп ко всему, кроме текста.** `set_style` и `move_after`
      давали пустой diff → ветка «текст не изменился» → откат ВСЕГДА. Две
      операции контракта были мертвы. Теперь в diff входят стиль, уровень,
      список и перемещение (перемещение — только когда множество id совпадает,
      иначе это insert/delete, а не move). Дало ColBERT 9 и Математика 4,
      обе подтверждены оценщиком.
- [x] **Модель не видела оформления, хотя `doc_map` его уже считал.**
      `render` выбрасывал `runs`(b/i/u), `level`, `list`. Правки вида
      «заголовок это просто жирный абзац» были невыполнимы по построению.
      Оформление ушло ВНУТРЬ скобки тега (`p0 [Normal, H0, весь жирный]`),
      текст после скобки остался дословным — иначе модель копирует маркеры
      в `old` и валидатор её отвергает. `outline` получил короткий `[B]`.
- [x] **`already` не отличал «уже так» от «не умею».** rule-путь знает два
      класса; правка вне класса давала пустой diff и вердикт «уже сделано».
      Теперь пустой diff у rule не терминал: цикл перезапускается обычным
      локальным путём, и `already` законен, только если и Редактор согласен,
      что менять нечего.
- [x] **Якорь искался точной подстрокой, а человек цитирует неточно.**
      11 якорей двух корпусов не находил ни точный, ни пробельно-гибкий
      поиск: в правке «IQ, уровень развития», в документе «IQ — уровень
      развития». Добавлен `find.locate` — совпадение по токенам (порог 0.75,
      от 3 токенов), ТОЛЬКО для навигации; мутация (`validate`,
      `_op_replace_text`) осталась строгой. Отдаёт РОВНО один лучший блок:
      жадный вариант замерен и отвергнут (3 выигрыша против 6 проигрышей,
      раздувал фрагмент до 9 блоков на одну фразу).
- [x] **Редактор пасовал перед объёмом, а не перед смыслом.** Отказы дословно:
      «правка требует синхронного изменения нескольких блоков (p87, p88, p96)».
      Цели режутся кодом на кластеры соседства, вызов Редактора на кластер,
      `doc_map` пересчитывается перед каждым. Транзакция ОСТАЛАСЬ одна на
      правку: один снимок, один diff, один Проверяющий, один откат — правка
      «переименуй везде», выполненная частично, оставляет документ
      противоречивым. Верхней границы числа кластеров нет (решение владельца:
      вызовов не жалко, важна последовательность).
- [x] **Полоса Редактора не охранялась кодом.** `validate` звали с картой
      ВСЕГО документа, поэтому операция по любому существующему id проходила,
      хотя промпт обещает «id только из фрагмента». До кластеризации дефект
      был невидим. Теперь id операции обязан принадлежать показанному
      фрагменту; исключение одно — `replace_all` (документ-широкая по
      контракту). Блоки, созданные этим же батчем, в полосу входят —
      узнаются по разнице ключей `idx`. Дало ColBERT 4 и 19 (настоящая
      таблица Word), обе подтверждены оценщиком.
- [x] **`run_all.py` тратил деньги и был недетерминирован.** `edit_demo`
      дёргал живую модель. Закрыто фикстурой, проверено запуском с заведомо
      негодным ключом.

### Ф12. Порча текста не ловится кодом (найдено замером, чинится)

Инвариант 1 требует, чтобы детерминированное делал код. Сейчас единственная
защита от порванных слов — что Проверяющий заметит. Заметил 5 раз из 5, все
документы целы, но это везение, а не механизм.

- [ ] **Обрезание на 300 знаках рвёт слова, и модель рвёт по ним документ.**
      `render` усекал текст блока и дописывал «…». Модель копирует показанное
      в `old`: замеренный случай — `old` кончается на «которые требуют с»,
      ровно на 300-м знаке. `_flex_span` находит эту строку (она ДЕЙСТВИТЕЛЬНО
      есть в документе), заменяет, и хвост слова «ерьёзных вычислительных
      ресурсов» остаётся приклеен к новому тексту. Длиннее 300 знаков — 50 из
      116 абзацев ColBERT и 39 из 52 Математики. Этот же дефект объясняет
      отказы «в p12 нет текста «…чис…»» и срабатывание защиты от многоточия.
      Убрать усечение в `render` (фрагмент теперь 2–3 блока, бюджетного
      основания нет); `outline` не трогать — он покрывает ВЕСЬ документ.
- [ ] **Операция бьёт по выводу предыдущей операции того же батча.**
      Замерено: 1) «кросс-энкодеров» → «кросс-энкодеров (Cross-Encoders)»,
      2) «Cross-Encoder» → «кросс-энкодеров» попала ВНУТРЬ вставки шага 1 →
      «кросс-энкодеровs». Тот же класс: `replace_all` «Bi-encoder» →
      «Bi-encoders» → «Bi-encoderss». `new`, содержащий `old`, сам по себе
      законен (добавление скобки) — дефект в ПОСЛЕДУЮЩЕЙ операции.
- [ ] **Разностная проверка порванных слов, без словаря.** Страховка после
      устранения причин: слово, появившееся в результате, которого не было в
      документе до правки и нет в тексте правки, — подозрительно. Словарь
      общего назначения (Орфо, hunspell) не годится и вреден: `MaxSim`,
      `nDCG@10`, `ColPali`, `эмбеддинги` законны, ложные срабатывания утопят
      сигнал. Разностная форма не требует ни словаря, ни зависимости.

### Ф13. Полнота правки и общий контекст

- [ ] **Правки на единообразие теряют общую картину.** ColBERT 7 и 15 упали
      на `old == new`: правка просит «выбери одно написание и поставь везде»,
      а Редактор видит один кластер и «унифицирует» его к тому написанию,
      которое в нём и стоит. Прямое следствие дробления. Лечится не откатом
      дробления, а инвентарём: код собирает варианты с частотами ДО цикла
      (`by_text`/`by_regex` уже есть) и кладёт в каждый вызов —
      «Варианты в документе: «Bi-encoder» ×7, «Bi-Encoders» ×2». Канонический
      вариант выбирается один раз и виден на каждом шаге.
- [ ] **`done` при частично выполненной составной правке.** Единственная
      оставшаяся ложь: ColBERT 1 содержит четыре требования ([CLS], [SEP],
      [MASK] и переписать фразу), выполнено одно, вердикт `done`. Проверено
      чтением файла. Механическая форма §7 присланной архитектуры: набор целей
      фиксируется ДО мутации, `done` требует, чтобы каждый кластер либо дал
      изменение, либо явно сказал «здесь нечего делать». Частичное закрытие
      перестаёт быть `done` по построению, а не по мнению Проверяющего.

### Ф13-бис. Найдено попутно, не чинится по симптому

- [ ] **Ретрай Редактора перезапускает батч с нуля поверх уже применённых
      операций.** `_apply_ops` на невалидной операции берёт НОВЫЙ список ops и
      идёт с индекса 0, а операции, успевшие примениться до сбоя, остаются на
      документе. Если модель в ответе повторит уже применённую операцию, она
      применится второй раз поверх изменённого текста. Найдено агентом при
      написании демо на самопопадание (демо специально обходит этот случай,
      цитируя нетронутый кусок). Живого проявления пока не замерено — держать
      как известный риск, чинить, когда появится доказательство или когда
      ретрай начнут трогать по другой причине.
- [ ] **Переполнение контекста модели на накопительном прогоне.** Сервер вернул
      `ContextWindowExceededError: Input (32022 tokens)`; документ в этот момент
      21 тыс. знаков, самый крупный кластер 1,6 тыс. — числа не сходятся, и при
      повторе того же шага на том же состоянии всё проходит. Триггер НЕ
      установлен, защиту по догадке не ставим. `llm.py` научен показывать тело
      ответа сервера и размер запроса, чтобы следующее падение объяснило себя
      само.

### Ф14. Учиться у чужих систем (исследование, потом решение)

Три системы найдены и бегло просмотрены; нужен предметный разбор с тем же
критерием отбора, что и всегда: идея принимается, только если её
необходимость доказывается из НАШЕГО кода или замера.

- [ ] **SuperDoc** (`docs.superdoc.dev/ai/agents/llm-tools`) — ближайший
      аналог, Word в вебе с инструментами для LLM. Совпало независимо:
      адресация по id элементов, а не по смещениям. Взять на разбор:
      *windowed reads* (`blockOffset`/`blockLimit` вместо усечения) — прямой
      ответ на Ф12; типизированные коды отказа (`MATCH_NOT_FOUND`,
      `REVISION_MISMATCH`) вместо русской прозы, чтобы цикл ветвился машинно;
      ограничение длинных списков С ЯВНЫМ счётчиком, а не молча; два уровня
      инструментов (`inspect` + `perform_action` с 40 действиями).
- [ ] **DocEdit-v2** (arXiv 2410.16472) — Doc2Command, порождение команд
      правки и семантическая привязка области через маскированную сегментацию.
      Разобрать, что из привязки области применимо к нашему `_resolve`.
- [ ] **Agent-DocEdit** (OpenReview 1ba209BACA) — привязка инструкции к
      структуре документа, генерация ПРОГРАММЫ вызовов заранее определённых
      API. Наш контракт из десяти операций — тот же принцип; смотреть, что у
      них устроено лучше.
- [ ] Отдельно замерено чужими: деградация выбора инструментов начинается с
      20–25 инструментов, при ~200 точность 41–83%; есть позиционный эффект —
      инструменты в середине списка выбираются реже. У нас десять, каталог
      (progressive disclosure) НЕ НУЖЕН сейчас и станет обязательным ровно
      при переходе за двадцать — то есть на Ф15.

### Ф15. Новые операции (потолок корпусов)

Без них 100% на приёмочных корпусах недостижимы, и это не дефект, а граница:
система честно отказывается. Решение владельца: ограничений на операции нет.

Общий принцип, подтверждённый разбором SuperDoc: у них сноски и библиография
ЕСТЬ в нижнем Document API и просто не выставлены в AI-слой. Значит это
инженерная работа с OOXML, а не открытая проблема.

- [x] `set_format` (начертание runs на диапазоне) и `set_list_level` — две
      малых операции. Написаны в конце сессии, В ЗАМЕРЕ НЕ БЫЛИ. Закрывают
      Математику 5 («курсивом в кавычках») и часть ColBERT 15. Обоснование их
      дешевизны: `parse.doc_map` УЖЕ читает и `runs`(b/i/u), и `list`, и
      `render` их УЖЕ показывает — модель эти свойства видит и не может
      менять; закрывается именно эта асимметрия.
- [ ] **Сноски Word** (Математика 19). Средняя: новая часть OPC
      `footnotes.xml` плюс связь в `.rels` плюс `w:footnoteReference` в теле.
- [ ] **Поля для автонумерации ссылок и список литературы** (ColBERT 20).
      Средняя, и самая хрупкая: сырой `w:fldSimple`. В «Известных рисках»
      она значилась последней ещё до всех замеров — не менять этот порядок.
- [ ] При переходе за 20 операций — каталог по Ф14: компактный список,
      полная схема только по запросу. Сейчас операций 12, порог не достигнут,
      РАНЬШЕ ВРЕМЕНИ НЕ ДЕЛАТЬ (чужие замеры: деградация начинается с 20–25).

### Ф16. Что осталось из 14 невыполненных правок (разбор на 2026-07-27)

Разложено по причинам на состоянии Ф12 (26 из 40 сделано изолированно).
Порядок работы — сверху вниз, но только после чтения результатов `w7`.

- [ ] **Редактор отказался — 7 правок** (ColBERT 7, 13, 16; Математика 13, 14,
      15, 18). Самая крупная и наименее разобранная кучка. ColBERT 7 — правка
      на единообразие, её должен закрыть инвентарь вариантов из партии `w7`.
      Остальные — смысловые переписывания. **НЕ считать это потолком модели
      заранее:** все четыре правки раздела «Сложные», про которые план
      предупреждал «ожидаемый исход — через раз», после снятия обрезания
      ВЫПОЛНЯЮТСЯ. Модель не упиралась в свой предел — ей показывали обрубок.
      Сначала разобрать по одной, чем именно недоволен Редактор.
- [ ] **Промах поиска — 3 правки.** ColBERT 4 («не нашёл, где это править» —
      проценты и числа, у которых нет ни одного текстового якоря), ColBERT 5
      и 8 («текста нет во фрагменте»). Это поиск, а не модель. Для правок по
      числам нужен якорь другого рода — регулярное выражение по образцу из
      текста правки, а не дословная цитата.
- [ ] **Нет операции — 3 правки.** Закрываются Ф15.
- [ ] **Оставшиеся смысловые** — если после всего выше правка не идёт, честный
      отказ является ПРАВИЛЬНЫМ исходом (мерило: не «20 из 20», а «вердикт не
      врёт»). Реалистичный потолок этих корпусов — 34 из 40 (85%).

## Чего в этом проекте нет (решено, не переоткрывать)

Отрезано осознанно, чтобы не повторить утопание первой попытки. Возврат
к любому пункту — только через архитектора, с названной правкой, которая
без него не делается.

- Универсальный Document Graph с адаптерами под PPTX/XLSX/LaTeX/HTML.
  Формат один — DOCX, и правится он на месте.
- Эмбеддинги, BM25, векторный индекс. Поиск — подстрока и regex: все 20
  правок имеют лексический якорь, документ 35 тыс. знаков.
- Отдельные индексы (Heading/Term/Citation/Formula/Image/Style Index) и
  Semantic Memory. Из всего этого нужна одна функция `first_mention`.
- Планировщик как отдельная модель. Список правок уже структурен,
  делится регуляркой.
- Параллельное исполнение правок. Правки идут по одному документу и видят
  результат предыдущих.
- Резервный провайдер, абстракция под смену модели, ротация логов,
  Patch Engine с разрешением конфликтов и проверкой циклов.

## Известные риски

- Правки 16–18 (перепутанные `m`/`n` в формулах, `⊤` как транспонирование,
  argmax против недифференцируемости max) требуют предметного рассуждения.
  Ожидаемый исход — через раз; архитектурно закрывается честным отказом.
- Правка 15 («список поехал, характеристики должны быть вложенными»)
  невыполнима буквально: в документе НЕТ списков — все 116 абзацев идут
  стилем `Normal`, маркеров списка ноль, а стиля `List Bullet` в каталоге
  документа нет (есть `List Paragraph`). Правильный исход — честный отказ
  с объяснением. Если система «сделает» эту правку, она соврала.
- Правка 20 (поля Word для автонумерации ссылок) — сырой OOXML,
  `w:fldSimple`. Самый хрупкий пункт, идёт последним.
- `quotes` расставляет ёлочки чередованием внутри абзаца. В приёмочном
  документе абзацев с нечётным числом прямых кавычек нет, поэтому здесь
  это безопасно; в чужом документе непарная кавычка станет открывающей.
  Косметика, кодом не лечим.
- Счёт aitunnel ~140 ₽. Полный приёмочный прогон — 2–3 ₽.
